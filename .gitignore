# Local app runtime data — not used by the cloud app (Supabase holds it)
data/uploads/
data/cmas/
# Scratch / reference artifacts
_ref/
# Python
__pycache__/
*.pyc
# OS
Thumbs.db
.DS_Store
# Cloud build: Puppeteer test scaffolding + the local handoff (has temp passwords) — never push these
node_modules/
package.json
package-lock.json
verify_cloud.mjs
CLOUD_DEPLOY_AND_HANDOFF.md

# Realtor's local branding (headshot/contact) — cloud uses Supabase, keep out of repo
data/settings.json
# Internal dev handoffs (keep out of the public repo)
RESET_AND_ADMIN_HANDOFF.md

# Local-only security audit write-up (documents a still-open item + history PII pointer)
SECURITY_AUDIT.md
